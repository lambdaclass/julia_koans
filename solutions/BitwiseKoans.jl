module BitwiseKoans

function not(x)
    ~x
end

function and(x, y)
    x & y
end

function or(x, y)
    x | y
end

function xor_koan(x, y)
    xor(x, y)
end

function right_shift(x, y)
    x >> y
end

function left_shift(x, y) 
    x << y
end

end